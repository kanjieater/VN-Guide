{
  "name": "vn-guide",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.2",
  "scripts": {
    "generate": "bun tools/generate-cli.mjs",
    "generate:check": "bun tools/generate-cli.mjs --check",
    "test": "bun test --concurrent --max-concurrency=20",
    "test:coverage": "bun test --coverage --concurrent --max-concurrency=20",
    "coverage:diff": "bun tools/check-diff-coverage.mjs"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
